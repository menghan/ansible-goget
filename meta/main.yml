---
galaxy_info:
  author: Menghan Zheng
  description: A role to download and compile golang binary for Ubuntu and Debian.
  license: Apache v2.0
  min_ansible_version: 2.2
  platforms:
    - name: Debian
      versions:
        - jessie
        - stretch
    - name: Ubuntu
      versions:
        - trusty
        - xenial
        - zesty
  galaxy_tags:
    - golang
    - development
    - packaging
    - system
dependencies: []
  # List your role dependencies here, one per line. Only
  # dependencies available via galaxy should be listed here.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
